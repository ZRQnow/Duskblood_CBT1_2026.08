# 05_drafts：草稿

存放文章、报告、视频脚本、图注和修订中的工作版本。草稿可以不完整，但必须保留来源编号和版本信息。

建议命名：

```text
YYYYMMDD_文档主题_v001_状态.ext
```

状态可用 `wip`、`review`、`revised`。例如：`20260821_cbt1-overview_v001_wip.md`。

- 每轮重要评审后递增版本号，不覆盖已发送评审的版本。
- 衍生裁剪图、标注图或短视频应与草稿同目录，并注明父级 `source_id`。
- 通过全部质检后，将定稿复制到 `07_final/`；草稿目录保留工作历史。

