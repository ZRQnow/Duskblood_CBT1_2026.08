# 07_final：终稿

这里只保存通过 `06_qc/` 检查、可按既定范围交付或发布的最终版本。

- 文件名建议：`YYYYMMDD_文档主题_v版本_final.ext`。
- 每个终稿都要在 `release_manifest.json` 中登记版本、哈希、质检记录和发布状态。
- 若终稿发布后修改，创建新版本，不覆盖旧版；旧版状态改为 `superseded`。
- 终稿引用的素材仍保留在原始目录，不在这里重复堆放大文件，除非交付包明确要求。

